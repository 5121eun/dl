#!/usr/bin/env python
# coding: utf-8

from setuptools import setup, find_packages

setup(
    name='models',
    packages = ['models'],
    version='0.0.9',
    url='https://github.com/5121eun/models.git',
    install_requires=[
        'torch'
    ]
)
